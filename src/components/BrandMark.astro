---
import { Image } from 'astro:assets';
import seal from '../assets/brand/paperseal-seal.png';
import { copy } from '../content/copy';

interface Props {
  compact?: boolean;
}

const { compact = false } = Astro.props;
---
<span
  class:list={['brand-mark', { 'brand-mark--compact': compact }]}
>
  <Image
    class="brand-mark__seal"
    src={seal}
    widths={[64, 96, 128]}
    sizes="64px"
    alt=""
    loading="eager"
  />
  <span class="brand-mark__type">
    <span class="brand-mark__name">{copy.shared.brand.name}</span>
    <span class="brand-mark__descriptor">{copy.shared.brand.descriptor}</span>
  </span>
</span>
